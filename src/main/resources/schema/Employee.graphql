scalar Date

type Employee {
    name: String
    email: String
    phone: String
    workAge: Int
    beginDate: Date
}

input EmployeeInput {
    id: Int
    name: String
    email: String
    phone: String
    workAge: Int
    beginDate: Date
}